import { Box, Typography } from "@mui/material";

const Logs = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="h5">Logs</Typography>
    <Typography>
      A log of all collected trades and events will be here.
    </Typography>
  </Box>
);

export default Logs;
