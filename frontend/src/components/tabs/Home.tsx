import { Box, Typography } from "@mui/material";

const Home = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="h5">Home</Typography>
    <Typography>The main data grid will be built here.</Typography>
  </Box>
);

export default Home;
