import { Box, Typography } from "@mui/material";

const Data = () => (
  <Box sx={{ p: 2 }}>
    <Typography variant="h5">Data</Typography>
    <Typography>
      Detailed statistics and historical data will be shown here.
    </Typography>
  </Box>
);

export default Data;
